#import "_theme.typ": *
#show: bem-slides.with(
  title: [Indo para 3D],
  subtitle: [Última aula · 2D ≈ 3D],
)

= Mensagem

== 2D e 3D são semelhantes

#keybox[
  `format*` → `geometric_props` → `H_G_full_direct` → `solve`
]

Mudam dimensão de $Gamma$, SF e nº de DOFs — não a lógica.

== O que muda

- curva → superfície
- `format2d` → `format3d`
- $P,A$ → $A,V$
- elast.: 2 → 3 DOFs

== Labs

G: geo no cubo · L: Laplace $T = z$

== Exercícios finais (3)

1. Geometria
2. Laplace
3. Elasticidade (patch 3D)

Em cada um: paralelo 2D ↔ 3D explícito.
